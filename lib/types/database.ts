// PLACEHOLDER. Regenerate this file once the Supabase project is linked:
//
//   npx supabase link --project-ref <project-ref>
//   npx supabase gen types typescript --linked > lib/types/database.ts
//
// Until then, query results are typed as `any` — typecheck still passes but
// you won't get IDE autocomplete on row columns. After the first generation,
// re-run the command after every migration to keep types in sync.

/* eslint-disable @typescript-eslint/no-explicit-any */
export type Database = any;
